# k6 压测结果

> 脚本：[scripts/k6/](scripts/k6/)（smoke / photo-list / upload / share 4 场景）
> 原则：只记录真实环境跑出的数据，不提交伪造数字。upload 已实跑，其余待跑。

## 结果

| 场景 | 日期 | 环境 | 数据规模（照片数） | VU × 时长 | p95 | 错误率 | 阈值 |
|------|------|------|------|-----------|-----|--------|------|
| smoke | — | — | — | — | — | — | — |
| photo-list | — | — | — | 20 × 60s | — | — | p95 < 300ms |
| upload | 2026-08-06 | 本地 dev | 100（种子化新照片） | 5 VU × 100 迭代（~5s） | 5.58ms | 0%（102/102 请求） | rate < 1% ✓ |
| share | — | — | — | — | — | — | — |

## 说明

- photo-list 数字必须注明当时照片库规模——空库只能证明「接口快 + 缓存命中」，支撑不了「万张照片不卡」
- upload 场景使用种子化图片（字节随 seed 变化）测真实写入吞吐，会真实落库/落盘，仅对测试环境执行；跑完需清理（删库中 `k6-%` 记录与 `photo.upload-dir` 下 `*_k6*` 文件）
- 认证端点限流 10 req/s/IP：每个场景在 `setup()` 中只解锁一次（HTTP 不能在 init context 发起）
