spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/photodb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
  cache:
    type: redis
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 2
  rabbitmq:
    host: ${RABBIT_HOST:localhost}
    port: ${RABBIT_PORT:5672}
    username: ${RABBIT_USER:admin}
    password: ${RABBIT_PASS:}
    listener:
      simple:
        retry:
          enabled: false  # 由 consumer 自行处理重试逻辑

photo:
  upload-dir: /data/photo-uploads
  processing:
    type: rabbitmq
  watermark:
    font: SansSerif
    font-size-ratio: 40
    color-alpha: 180

cors:
  allowed-origins: http://localhost:*,https://hape233.online

# 限流/封禁的客户端 IP 来源：仅信任 cloudflared 隧道覆写的 Cf-Connecting-Ip
# （app 只绑 127.0.0.1，客户端无法伪造）；换 nginx 等反代时改为 X-Real-IP，留空则不信任任何头
security:
  trusted-proxy-header: Cf-Connecting-Ip

# 生产环境关闭 API 文档
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

# 暴露 health（Docker healthcheck）+ prometheus（监控）
management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  metrics:
    export:
      prometheus:
        enabled: true

# /actuator/prometheus 的 Basic Auth 凭据（SecurityConfig 条件加载 MONITOR 用户；
# 漏配时 ProdSecurityValidator 启动即失败，杜绝静默裸奔）
monitoring:
  username: ${MONITORING_USER:}
  password: ${MONITORING_PASSWORD:}
