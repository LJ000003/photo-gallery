# Docker Compose 环境变量模板 — 复制为 .env 并修改
# cp .env.example .env

# MySQL
MYSQL_ROOT_PASSWORD=请替换为随机密码
MYSQL_DATABASE=photodb

# App
DB_HOST=mysql
DB_USERNAME=root
DB_PASSWORD=${MYSQL_ROOT_PASSWORD}
JWT_SECRET=请替换为 openssl rand -base64 32 的输出
ADMIN_PASSWORD=你们朋友间的共享密码
